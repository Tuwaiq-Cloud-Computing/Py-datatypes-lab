

Q1 /

# Make a list with 3 elements
my_list = [1, 2, 3]

# Add an element to the end of the list
my_list.append(4)

# Remove an element from the list
my_list.remove(4)

# Reverse the list
my_list.reverse()

# Sort the list
my_list.sort()

# Add an element at the start of the list
my_list.insert(0, 0)

# Print the index of the last element
print( len(my_list) - 1)

Q2/


# Consider the following list: people = ["Ahmed", "Nasser", "Mohammed"]
people = ["Ahmed", "Nasser", "Mohammed"]

# Print the list in the following format: Ahmed, Nasser, Mohammed
print(", ".join(people))


Q3/

# Make a list of 3 dictionaries, each dictionary should contain information such as: name, phone_number
person_list = [
    {"name": "Ahmed", "phone_number": "123456789"},
    {"name": "Nasser", "phone_number": "987654321"},
    {"name": "Mohammed", "phone_number": "1122334455"},
]

# Add one more dictionary to the list
person_list.append({"name": "Ali", "phone_number": "555555555"})

# Delete the name from the first dictionary
del person_list[0]["name"]

# Update the phone number of the last person
person_list[-1]["phone_number"] = "666666666"

# Check if the first dictionary has a key called "name"
if "name" in person_list[0]:
    print("The first dictionary has a key called 'name'")
else:
    print("The first dictionary does not have a key called 'name'")
